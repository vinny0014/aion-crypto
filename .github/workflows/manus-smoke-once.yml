name: AION Manus smoke test (retired)

# The one-time production smoke test completed successfully on 2026-08-14.
# Evidence is stored in the private coordination ledger as task 2.
# This workflow intentionally has no schedule or push trigger.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  evidence:
    runs-on: ubuntu-latest
    steps:
      - name: Report retired test
        run: |
          echo "Codex-Manus round-trip verified: task created, claimed, heartbeat received and completed."
          echo "Automatic reruns are disabled to prevent duplicate messages."
