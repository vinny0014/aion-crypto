import type { Metadata } from "next"; import PolicyPage from "../../components/PolicyPage";
export const metadata: Metadata = { title: "Cookie Policy", description: "How AION Crypto uses essential storage and analytics cookies.", alternates: { canonical: "/cookie-policy" } };
export default function Page(){return <PolicyPage title="Cookie Policy" intro="AION Crypto uses only storage needed for security, sign-in, watchlists and measurement that is explicitly configured." sections={[["Essential storage","Session and watchlist storage supports requested features and cannot be used to buy, sell or access an exchange account."],["Analytics","Google Analytics runs only when its public measurement ID is configured. Advertising cookies are not active before AdSense approval and consent controls."],["Your control","You can clear browser storage at any time. Where consent is legally required, optional cookies must remain off until you choose them."]]}/>}
